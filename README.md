# Lexical-analyzer-project
A Lexical Analyzer (Scanner) implemented in C that reads source code as input and breaks it into meaningful tokens such as keywords, identifiers, operators, literals, and special symbols. This project demonstrates the fundamental phase of a compiler.
